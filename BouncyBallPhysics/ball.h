struct ball_t {
  int mass;         // Mass in kilogrammes
  float velocity;   // Velocity in meters / second
  float height;     // Height above the ground in meters
  float elasticity; // Elasticity is a dimensionless number indicating
                    // the kinetic energy retained during bouncing.
};
